#ifndef HYPERGRAPH_H
#define HYPERGRAPH_H
#include <iostream>
#include <vector>
#include <queue>
#include <string>

class Hypergraph{
	private:
	vector<vector<int>> edgeLists;
	vector<int> adjVertices;
	vector<string> adjEdges;
	public:
		
}

#endif

